{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  // Duplicated from tsconfig.app.json so tooling that reads only this file
  // (the shadcn CLI) can resolve the @/* alias.
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
